# Contributing

Community guards must stay declarative and use only VibeBloat's trusted action library.

- Add focused `bun:test` coverage with every behavior change.
- Never add executable code to a shared guard.
- Never commit secrets, transcripts, or runtime state.
- Keep commits small and scoped.
